from plyer import notification

title = 'Hello!'
message= 'Thankyou for reading! Take care!'

notification.notify(title= title,
                    message= message,
                    app_icon = None,
                    timeout= 20,
                    toast=False)
